export interface GameItem {
  id: string;
  title: string;
  description: string;
  genre: string;
  cover: string;
  link: string;
  date: string;
  players: string; // e.g., "1 Player", "2 Players"
}
