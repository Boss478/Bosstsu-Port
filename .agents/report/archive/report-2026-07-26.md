# Session Report: 2026-07-26

## Scope
Full-stack refactor + animation polish across non-game surface.

## Accomplishments

### 1. Animation Polish V2 (Phases 1-3) — 12 tasks
- Header dropdown hover animation (slides down/up, `transition-all duration-300 ease-out`)
- 4 modal entrances (fade-in backdrop + scale-up/popup-enter content)
- Card press feedback (`active:scale-[0.98] active:shadow-lg`)
- Button/link press feedback (`active:scale-95` ± `active:shadow-lg`)
- CookieConsent show/hide symmetry (800ms, direction swap)
- Scoped `transition-all` → `transition-[color,background-color,transform,box-shadow]`

### 2. Council on Animation Durations (60fps.design cross-reference)
- **Verdict**: Ship Phases 1-3, defer Phase 4 (scroll-reveal)
- **Decisions**:
  - Press feedback: 300ms→150ms (60fps says "snappy")
  - CookieConsent: 800ms symmetric, direction inverted (in=slide-up, out=slide-down)
  - `transition-all` → scoped properties to avoid compositor tax

### 3. Zombie Transition Cleanup
- Removed 12 dead `transition-all` instances across 7 files
- Pattern: `transition-all duration-200 transition-colors duration-150 transition-transform duration-75` → `transition-transform duration-75`

### 4. Dead Animation Utility Removal
- Removed 8 unused `@utility` + `@keyframes` blocks from globals.css (~80 lines)
- Removed: `card-flip-enter`, `count-up`, `fade-left`, `fade-right`, `float-island`, `glitch-reveal`, `pulse-ring`, `wrong-shake`
- Kept `@keyframes glitch-reveal` (referenced by companion system)

### 5. Listing Client Refactor
- Created 5 shared artifacts:
  - `useListFilter` hook (debounced search + client filter + page handler)
  - `SearchInput` component
  - `FilterBar` component
  - `SortSelect` component
  - `LISTING_IMAGE_SIZES` + `@utility grid-section`
- Migrated all 3 listing clients (Portfolio, Gallery, Resources)
- Fixed Resources `allLabel: 'All'` bug
- ~167 lines of inline duplication removed

### 6. Hydration Mismatch Fix
- Root cause: stale `.next` cache after `className` changes from zombie cleanup
- Server HTML had old class names; client bundle had new class names
- Fix: clean build + rebuild

## Bugs Fixed
| Bug | Root Cause | Fix |
|-----|-----------|-----|
| Resources dirty URL (`?type=All`) | `useListNavigation` default `allLabel='ทั้งหมด'` ≠ Resources' `'All'` filter value | Added `allLabel: 'All'` |
| Hydration mismatch | Stale `.next` cache after class name changes | Clean build |
| `useListFilter` lint error | Ref syncing in render body (React strict mode) | Moved to `useEffect` |

## Files Touched
- **New**: 5 files (useListFilter, SearchInput, FilterBar, SortSelect, constants)
- **Modified**: ~20 files across components, hooks, CSS, config
- **Deleted**: None

## Deferred
- Phase 4: wire `useScrollReveal` + remove unused CSS utilities
- Listing refactor `useListFilter` scrutiny Finding #1 (ref closure behavior change — documented)
