# Task Report — Apr 19, 2026 21:15

## Task
Add WYSIWYG ↔ HTML Code view toggle to `RichTextEditor.tsx`.

## Version
`1.5.16` → `1.5.17`

## Changes

### `src/components/admin/RichTextEditor.tsx`

**Added:**
- `ViewMode` type: `'wysiwyg' | 'html'`
- `viewMode` state (default `'wysiwyg'`)
- `htmlCodeRef` (`useRef<HTMLTextAreaElement>`)
- `toggleViewMode()` function:
  - WYSIWYG → HTML: copies `editorRef.current.innerHTML` into textarea, closes open panels
  - HTML → WYSIWYG: writes textarea value back into `editorRef.current.innerHTML`, calls `syncHidden()`
- Toggle button at far-right of Row 1 (`ml-auto`) — always visible regardless of mode
  - In WYSIWYG: amber hover style + `fi-sr-code` icon + "HTML" label
  - In HTML mode: sky active style + `fi-sr-pencil` icon + "Editor" label
- HTML `<textarea>` element (monospace, `spellCheck={false}`, same height constraints as editor)

**Conditionally hidden in HTML mode:**
- All Row 1 formatting buttons (block format, font size, B/I/U/S, color, alignment)
- Row 2 entirely (lists, indent, link, image, HR, clear)
- Link and image inline dialogs

**Hidden input stays in sync** in both modes — form submission works identically.

## Lint
No new lint errors. 6 pre-existing problems in `PythonCompilerClient.tsx` unchanged.
