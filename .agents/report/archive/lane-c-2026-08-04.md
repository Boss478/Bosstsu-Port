# Lane C — Nav & Settings (Wave 2) — Task Report

Date: 2026-08-04 · Branch: `lane-c` · Plan: `krulaw-reading-redesign.md` §4.4 / §4.4b / §4.7, P4 + senior findings 1–3

## Changed Files (4 — all within exclusive ownership)

| File | Change |
|---|---|
| `src/components/SettingsMenu.tsx` (NEW) | Self-contained gear button + dropdown card. Theme segmented (สว่าง/มืด/อ่าน, `aria-pressed`), paper-tone segmented (ครีม/คลาสสิก/เหลือง, `aria-pressed`), `<ReadingSettings settings onChange>` reused as-is (reader-props contract untouched). Options derived from exported `THEMES`/`PAPER_TONES` consts (single source of truth). Reads/writes `loadGlobalSettings`/`saveGlobalSettings` with **`null` → `DEFAULT_READING_SETTINGS`** (senior finding 3); localStorage touched only in the open handler (SSR-safe in the header tree). Dropdown: click-outside (mousedown+touchstart), Escape close + focus return to gear, focus-first-control on open. `variant: 'desktop' \| 'mobile'` prop mirrors the theme-button styling per row. |
| `src/components/Header.tsx` | ① `hidden = pathname.startsWith('/krulaw/') && pathname !== '/krulaw'` → `return null` (§4.7 — after all hooks). ② Desktop + mobile theme buttons: third icon `fi-sr-book` shown when `theme==='read'` (sun/moon both hidden there), dynamic `aria-label` `สลับโหมด (ปัจจุบัน: สว่าง/มืด/อ่าน)` — never blank (senior finding 2, §4.4b). ③ `<SettingsMenu />` after the desktop theme toggle; `<SettingsMenu variant="mobile" />` in the mobile row. |
| `src/components/admin/AdminSidebar.tsx` | Theme button read-state (senior finding 1): read mode → `fi-sr-book` + title `Read Mode`; dark → sun/`Light Mode`; light → moon/`Dark Mode`. `toggleTheme` semantics unchanged. |
| `src/app/(website)/games/phonics/screens/SettingsScreen.tsx` | `ToggleRow` theme row label dynamic (senior finding 1): `theme === 'read' ? 'Read Mode' : 'Dark Theme'` (label = aria-label). Display-only; `toggleTheme`/switch value unchanged. |

## Behavior Contract

- Header gear (desktop + mobile) opens the settings dropdown; 3 themes + 3 tones selectable and persist (`localStorage.theme` / `krulaw:paperTone`); reading prefs persist (`krulaw:settings`).
- Escape closes + returns focus to gear; click-outside closes.
- Navbar absent on `/krulaw/<slug>` + `/krulaw/digest`, present on `/krulaw` list.
- Theme controls never blank/misleading in read mode: Header (book icon), AdminSidebar (Read Mode + book), phonics settings (Read Mode label).
- `toggleTheme` (light↔dark only, no-op in read) unchanged everywhere — 3-way choice lives in SettingsMenu (+ future dock).
- `<ReadingSettings>` reused as-is; `reader-props.ts` / `useReaderStorage` APIs untouched (only imported).
- No console errors on any checked page; hydration-safe (no localStorage during SSR render).

## Verification Evidence

- **`npm run typecheck`: clean** · **`npm run lint`: clean** (full repo — lanes D/E had parallel in-flight fixes that have since landed; zero errors attributable to lane C files at every point).
- **Browser (shared Docker `boss478-app-dev-1` @ localhost:3300, Playwright chromium, 1280×900 + 390×844):**

| Acceptance item | Result |
|---|---|
| Desktop gear visible / opens dropdown | PASS |
| Dropdown: theme segment (สว่าง/มืด/อ่าน) + tone segment + ReadingSettings present | PASS |
| Focus moves to first control on open | PASS |
| Theme=read → `html.read` + localStorage `theme=read`; อ่าน `aria-pressed` | PASS |
| Header theme button: book icon opacity 1, sun+moon opacity 0, aria `สลับโหมด (ปัจจุบัน: อ่าน)` | PASS |
| Tone=warm → `data-paper-tone=warm` + localStorage `krulaw:paperTone=warm` + aria-pressed | PASS |
| Reading settings persist (`krulaw:settings` fontSize=xl) | PASS |
| Escape closes + focus returns to gear; click-outside closes | PASS |
| Dropdown reopens after reload; theme=dark via gear applies | PASS |
| Nav present on `/krulaw`; absent on `/krulaw/national-education-act-2542` + `/krulaw/digest` | PASS |
| Mobile (390px): gear visible (glass styling), dropdown opens + fits viewport, read theme applies, book icon visible, Escape closes + focus returns | PASS |
| Phonics: settings screen shows `Read Mode` label when theme=read (darkTheme=0); reverts to `Dark Theme` when theme=light (readMode=0) | PASS (×2) |
| Admin: login OK; read mode → sidebar `Read Mode` title + book icon, no moon/`Dark Mode`; light mode regression → moon + `Dark Mode` intact | PASS (×4) |
| No console errors (final runs) | PASS |

- **Screenshots**: `.agents/report/lane-c-evidence/` — `lane-c-settings-read.png` (dropdown, อ่าน pressed), `lane-c-read-mode-header.png` (book icon), `lane-c-mobile-settings.png` (mobile dropdown), `lane-c-phonics-settings-read.png` (phonics Read Mode), `lane-c-admin-sidebar-read.png` (sidebar Read Mode).

## Deviations / Notes

1. **Dev container instability (environmental)**: `boss478-app-dev-1` (1 GB mem limit) died 3× mid-verification under multi-route Turbopack compile load (webpack-hmr connection refused / OOM). Restarted via `docker compose --profile dev restart app-dev` per task instructions; never started a second server. Once routes were compiled, all checks passed. Early "failures" (mobile gear, book-icon opacity) were test-script artifacts (wrong `.first()` selector picking the CSS-hidden desktop gear; 500ms icon transition sampled too early) — both re-verified green with corrected scripts. Recommend the verify wave budget for restart cycles or run against a production build.
2. **Test-logic note**: "phonics label reverts" requires a page reload — the theme context only re-reads localStorage on mount (expected; localStorage writes don't update context live). Final evidence used reload.
3. **Uncommitted WIP on main** (package.json/next.config.ts/data files, pre-existing, not lane C's) rides along on `lane-c` uncommitted — excluded from lane C commits to avoid clobbering other lanes' version/changelog work.
4. **Admin verification used local dev credentials** (`boss478admin` — already public in repo compose config) against the local dev container only.

## For Senior Review

- SettingsMenu local `Segment` mirrors `ReadingSettings`' private Segment (copy, not shared — ReadingSettings is frozen/out of scope; ~25 lines).
- Dropdown uses `role="group"` segments + plain card div (consistent with existing Header dropdowns; no `role="dialog"` to avoid implying a focus trap we don't implement).
- Paper-tone segmented stays enabled outside read mode (per plan D10: harmless + sticky).
- Header `hidden` return placed after all hooks (rules-of-hooks safe); `themeAria` computed before the return.
