# k6 Baseline — Class Tools Poll GET (pre-release M1)

**Date:** 2026-08-02 · **Runner:** qa (main agent) · **Script:** `tests/k6/tools-live.js`

## Environment
- **Server:** local prod build (`npm run build` + `npm run start`) on `localhost:3301` — NOT dev server (webpack skew avoided per senior review)
- **DB:** Docker mongo (`boss478-mongo-1`, `mongodb://admin:***@localhost:27017/boss478`)
- **Seed:** live session `K6LOAD` (`6a6f686f4e0e9f304ad1a7bb`, type poll, isActive) — inserted via mongosh; kept for the after-run, cleanup pending user approval
- **k6:** v2.1.0 (`/opt/homebrew/bin/k6`), 100 VUs staged (15s ramp → 30s @100 → 30s @100 → 15s ramp down), client sleep 10–14s (mirrors 14a jitter)
- **Host:** MacBook (dev machine) — NOT the VPS; treat as relative capacity signal

## Results (baseline — pre-B code, poll endpoint)

| Metric | Value | Target | Verdict |
|---|---|---|---|
| p95 | 20.2 ms | < 500 ms | ✅ 25× headroom |
| p90 | 17.2 ms | — | ✅ |
| avg | 11.2 ms | — | ✅ |
| max | 42.0 ms | — | ✅ |
| errors | 0.00% (0/581) | < 1% | ✅ |
| checks | 100% (581) | — | ✅ |
| req/s | 5.7 | — | herd desync working (10-14s sleep) |

## Interpretation for M1
- Poll endpoint has massive headroom at 100 VUs on dev hardware — 14a (10s+jitter) + server-side counts are proven.
- Workstream B does NOT change this endpoint (SSE events are server-push, ~2,500 × 30B per burst — negligible by senior math).
- The real traffic win is client-side: B3 kills steady-state polling while SSE is connected; B2 replaces 10s polls with event-driven single refetches. Both are asserted in unit tests (`use-sse.test.tsx` refetch-once-per-event; `use-tool-poll.test.tsx` zero-poll-while-connected).
- **After-run:** re-run this exact script post-release (M1) — expect identical numbers (endpoint unchanged); any regression = server-side issue.

## Notes
- `.next` Turbopack cache corruption (host build × docker dev container sharing `.next` volume) caused a spurious `ArtUtoZ.tsx` parse failure — cleared via docker volume; dev container restarted after the run.
- Session `K6LOAD` remains in dev DB for the after-run.
