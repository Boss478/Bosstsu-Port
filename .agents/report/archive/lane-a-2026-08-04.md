# Lane A Task Report — P2 (3-mode theme) + P1 (41 icons)

Date: 2026-08-04 · Lane: A (junior-engineer-A) · Branch: `lane-a` (off `lane-tdd` a5de51d)
Commits: `ccb0e80` (P2) · `90a53a1` (P1)

## Changed Files

| File | Change |
|---|---|
| `src/components/ThemeProvider.tsx` | `type Theme='light'\|'dark'\|'read'`; new `type PaperTone`; exports `getInitialTheme()` (stored ∈ {light,dark,read} wins, invalid/empty → OS matchMedia fallback), `getInitialPaperTone()`; `setTheme(next)` persists `theme` + applies EXACTLY ONE of `.light/.dark/.read` on `<html>` (all three toggled explicitly — FR-B); `toggleTheme` stays light↔dark, NO-OPs from `read` (AdminSidebar / phonics SettingsScreen unchanged); `paperTone` ('soft'/'classic'/'warm', default 'classic', persisted at `krulaw:paperTone`, invalid → 'classic') + `setPaperTone` mirroring `data-paper-tone` on `<html>`; `useTheme()` now exposes `{ theme, setTheme, toggleTheme, paperTone, setPaperTone, mounted }` |
| `src/app/globals.css` | `@custom-variant read (&:where(.read, .read *));` next to dark variant; `:root.light` / `:root.read` (+ `color-scheme`) AFTER the OS media query, `:root.dark` kept; paper-tone vars per D10 (`html.read[data-paper-tone="soft/classic/warm"]` → `--read-bg`/`--read-card` #f5ecd9/#faf3e3 · #f2e8d5/#f7efdc · #f6efd9/#fdf6e3); `.read #main-content{background:var(--background)}`; `.read footer` (footer bg hard-coded `bg-blue-100`); `.read` overrides for `.krulaw-article-card/.krulaw-toc/.krulaw-panel/.krulaw-dock/.krulaw-list-card/.krulaw-digest-card` (bg `var(--read-card)`, border `#e0d5b0`, ink `var(--read-ink,#3a2f1f)`); `.read mark` amber 50%; `.read [data-krulaw-body]{color:var(--read-ink)}`; muted-text overrides `.read [class*='krulaw-'] .text-slate-400/500/600` → #8a7a55/#7a6845/#6b5b3c |
| `src/components/ArticleView.tsx` | `data-krulaw-body` div: added explicit `text-slate-800` (kept `dark:text-slate-200`) — light-fix; line ~220 `scroll-mt-24` → `scroll-mt-20` (P6 piece owned by this lane) |
| `src/app/layout.tsx` | Pre-hydration inline `<script>` in `<head>`: reads `localStorage.theme` (∈ light/dark/read, else OS matchMedia) + `krulaw:paperTone` (∈ soft/classic/warm, else classic), sets the single class + `data-paper-tone` before first paint; guarded try/catch; `suppressHydrationWarning` kept |
| `src/fonts/flaticon-subset.css` | +41 `.fi-sr-<name>:before{content:"\f…"}` entries (219 total); removed a pre-existing stray `-e ` sed-artifact line |
| `src/fonts/uicons-solid-rounded.subset.woff2` | Rebuilt via pyftsubset from the official font (all 219 codepoints, 209 unique): 31,032 B / 179 glyphs → 16,720 B / 210 glyphs |
| `src/fonts/uicons-solid-rounded.subset.ttx` | Regenerated (committed ttx was STALE — 248-line 118-cmap dump from the original 117-glyph era, did not match the committed 178-glyph woff2; new ttx = 209 codepoints × 2 cmap subtables, consistent with the woff2) |
| `src/app/(website)/krulaw/dev/icon-audit/page.tsx` | TEMPORARY audit page — created for browser verification, **DELETED** before finishing (route now 404s) |

## P2 — Behavior Contract

- Exactly ONE of `.light/.dark/.read` on `<html>` at all times (all three toggled explicitly — stored site theme beats OS scheme; OS-dark + site-light renders light, no white text).
- `getInitialTheme()`: stored valid → stored; stored invalid/empty → OS fallback; nothing stored → OS fallback.
- `toggleTheme()`: light↔dark only; from `read` it NO-OPs (context `theme` unchanged, localStorage untouched). AdminSidebar + phonics SettingsScreen consumers verified unchanged (same signature).
- Paper tone: default 'classic'; stored valid → stored; invalid → 'classic'; always mirrored on `data-paper-tone`.
- Pre-hydration script mirrors `getInitialTheme`/`getInitialPaperTone` exactly (no FOUC, no flash).
- Read mode is fully styled on KRULAW surfaces; elsewhere degrades to paper body + light components (documented trade-off, plan FR-A).
- `read:` Tailwind variant available for later lanes (dock, settings menu, digest FAB).

## P1 — Icon swaps (21 of 41 names don't exist in the official set — Flaticon renamed across versions)

Official source: `@flaticon/flaticon-uicons` **v2.4.0** — the version line whose CSS matches the repo's existing 178 codepoints **178/178** (v3.x re-assigned codepoints; 0/178 match). CDN `webfonts/uicons-solid-rounded.woff2` is byte-identical to the v2.4.0 font (md5 `dc1dcfe98ec1262f0bf62041d5023292`). 20 names exist EXACTLY; 21 mapped to nearest official glyph (aliased under the code's class name — no code changes needed, no fake codepoints):

| Code uses | Official glyph (v2.4.0) | Reason |
|---|---|---|
| sliders-h | settings-sliders | renamed |
| tool | tool-box | no `tool` |
| align-right | align-slash | **weakest swap — no right-align glyph exists in v2.4.0; flag for ui-ux-designer** |
| budget | chart-pie | no `budget` |
| code | brackets-curly | `code-branch` also exists; {} closer to "HTML code" (RichTextEditor) |
| drawer / empty | inbox | no `drawer`/`empty`; open-tray semantics |
| file-archive | archive | renamed |
| floppy-disk | disk | plain floppy (better than circle-arrow variant) |
| image | picture | renamed |
| info-circle | info | no `circle-info` |
| list-ol | list | no numbered-list glyph |
| paint-brush | brush | renamed |
| pin | thumbtack | `map-pin` also exists; thumbtack matches sticky-note usage (BroadcastBanner) |
| server | database | renamed |
| times | x | no `xmark`/`close` |
| times-circle | circle-xmark | renamed |
| triangle-fill | triangle | same glyph (triangle IS filled) |
| unlink | link-slash | renamed |
| user-settings | user-gear | renamed |
| warning | exclamation | no `triangle-exclamation` |

## Verification Evidence

**Unit tests** (`npx vitest run tests/unit/krulaw`): 30/30 pass — theme-provider.test.tsx **15/15 GREEN** (was RED on lane-tdd), reader-settings 15/15 (B's P3 tests pass only on lane-b — expected RED on lane-a without B's impl).
Full suite: 1074/1089 (the 15 fails = P3 tests, lane B's scope, green at A+B merge).

**typecheck**: pass · **lint**: pass (0 warnings) · **build** (`npm run build`): **PASS** clean (incl. `/krulaw/[slug]` SSG routes).

**Browser (shared dev server localhost:3300, Docker)**:
- Read mode: `<html class="read">` only; `--background:#f5ecd9` (soft); body + `#main-content` + footer = paper `rgb(245,236,217)`; text `rgb(58,47,31)`; article body ink.
- Tones: soft `#f5ecd9` / classic `#f2e8d5` / warm `#f6efd9` all applied on body AND main (exact plan values).
- Light: `class="light"` only, body `#ffffff` + `#171717`, article body `text-slate-800` (readable — FR-B fixed).
- Dark: `class="dark"` only, `#0a0a0a`/`#ededed`, article `slate-200` (no regression).
- Compiled CSS order verified: `:root.light/.dark/.read` all AFTER `@media (prefers-color-scheme: dark)` (specificity + order ⇒ stored theme always wins).
- Pre-hydration: class + `data-paper-tone` present on every load; console shows NO hydration errors (only pre-existing `/api/analytics` 500 — `ANALYTICS_SALT` unset, env issue).
- Icons (canvas pixel check on the temp audit page): **41/41 render** (0 blank); old-subset spot checks OK (book, heart, palette, plus, search, star, add, angle-left, settings, minus); outline scan of all 178 shared codepoints: **0 empty glyphs** in the new font; server serves the new 16.7 KB woff2 (md5 match) with fresh content-hashed URL.
- Screenshots: `.agents/report/lane-a-read-mode-reader.png`, `.agents/report/lane-a-icon-audit.png` (audit page since deleted).

## Known Risks / For Senior Review

1. **Branch hiccup (shared working dir)**: my P2 commit briefly landed on `lane-b` (lane B had switched the repo branch mid-session). Recovered: `lane-a` = a5de51d + P2 + P1; `lane-b` pointer restored to `bebc6cb` (B's P3 only) via `git update-ref` — working tree untouched. The stray commit `56c5bc6` is unreferenced (recoverable via reflog). **Lane B should verify their branch/working tree.**
2. **Dev server instability**: shared container's `/krulaw/[slug]` route currently 500s in DEV (`require is not defined` — dev-mode only; prod build passes). Root cause not in my files (home/list/digest/games all 200; reader route pulls lane D's in-flight KrulawReaderClient). Also: another lane changed `package.json` dev script (`next dev --webpack` → `next dev`, i.e. Turbopack default) and `next.config.ts` CSP comment — may interact. Recommend: lane D finishes → container restart → re-verify.
3. **Docker daemon crashed mid-session** (socket gone) — restarted Docker Desktop + app-dev; cleared stale `.next` volume once (webpack watcher had gone stale on the bind mount; fresh compile required). All lanes should be aware the dev server was restarted.
4. **align-right → align-slash** is the only visually imperfect swap (no right-alignment glyph exists in official v2.4.0 — FA5 name removed; `align-right-justify` also absent). Flag for ui-ux-designer; alternative: draw the toolbar button with a custom glyph or accept align-slash.
5. **Committed ttx was stale** (from the 117-glyph era) — my regenerated ttx is now consistent with the woff2; the ttx diff vs HEAD includes the stale-baseline delta (large) plus the intended 31 new glyphs.
6. **Subset size decreased** (31 KB → 16.7 KB) because the v2.4.0 source font is leaner than the older 2.x font the original subset used — content verified (cmap + outlines + browser render), so the size drop is safe; a small win.
7. Font source files (official v2.4.0 CSS/woff2) are in `/tmp/flaticon-full/` (not committed; re-fetchable via jsdelivr if needed).
8. Uncommitted working-tree changes from OTHER lanes (`.prettierignore`, `changelog.md`, `content/krulaw/*`, `src/data/krulaw/*`, `next.config.ts`, `package.json`, `.gitignore`) — untouched by me, NOT in my commits.

## Deviations from Plan

- Plan §4.9 said fetch CSS+woff2 from `cdn-uicons.flaticon.com/uicons-solid-rounded/` — the CSS path 404s on the CDN (only `webfonts/*.woff2` exists there); used the official npm package via jsdelivr instead (same official file, md5-verified against the CDN woff2).
- 21 names required glyph swaps (plan anticipated this: "fallback = nearest official glyph"); implemented as CSS aliases (no code changes) — cleaner than touching other lanes' files.
- `.read` muted-text rules use `[class*='krulaw-']` instead of the plan's literal `.krulaw-*` (which is invalid CSS).
- The plan's P2 included a Header read-icon (4.4b) — that file is lane C's; not touched (plan §8.5 file-ownership matrix assigns Header to lane C).
