# Task Report — v1.8.26 (2026-05-21)

## Completed
- Fixed filter buttons on /resources — `filterKey` `'tag'` → `'type'` (server expects `?type=`)
- Fixed filter buttons on /games — `filterKey` `'tag'` → `'category'` (server expects `?category=`)
- Fixed `handlePageChange` crash on /resources — `activeTag` → `activeType`
- Fixed `handlePageChange` crash on /games — `activeTag` → `activeCategory`
- Removed dead `handleSearchChange` function from GamesClient (referenced undefined `searchTimeoutRef`)
- Capped games grid at 3 columns max — removed `xl:grid-cols-4` to prevent play button overlap with tag badges
- Version bumped to 1.8.26, changelog updated
- Memory updated with Filter Button Bug Pattern

## Files Changed
- `src/app/(website)/resources/ResourcesClient.tsx` — 2 edits (filterKey + handlePageChange)
- `src/app/(website)/games/GamesClient.tsx` — 4 edits (filterKey + handlePageChange + remove dead code + grid cols)
- `package.json` — version 1.8.26
- `changelog.md` — v1.8.26 entry added

## Verification
- `npm run lint` — passes
- `npm run build` — passes