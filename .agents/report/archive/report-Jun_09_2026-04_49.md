# Report: Alphabet Adventure QoL Phase 2

**Date:** 2026-06-09 04:49
**Version:** v1.9.61
**Status:** Complete

---

## Summary

Implemented Phase 2 of Alphabet Adventure QoL: Wrong Answer System + Per-Letter Stats.

## Changes

| # | Item | Files | Status |
|---|------|-------|--------|
| 1 | Wrong choice red + disable (match & fill) | `useGameActions.ts`, `MatchLevel.tsx`, `GameScreen.tsx` | Done |
| 2 | Remove auto-advance on 2 wrongs | `useGameActions.ts` | Done |
| 3 | Remove `showCorrect` hint for fill levels | `useGameActions.ts` | Done |
| 4 | Per-letter error frequency on victory | `VictoryScreen.tsx` | Done |
| 5 | Expand `wrongLetters` to fill + typing | `useGameActions.ts` | Done |
| 6 | `wrongChoices` lifecycle (round gen, fill adv, typing) | `useGameActions.ts`, `types.ts` | Done |

## Verification

- `npm run build` — PASSED

## Key Decisions

- `wrongChoices` stored in `RoundData` (not `GameState`) because it resets per-round/ per-active-cell (fill). GameState's `wrongLetters` tracks cumulative errors across playthrough
- Red styling uses rose-500 (bg + border + shadow) — matches project's existing error palette
- Per-letter stats: expanded `wrongLetters` push to all level types, victory screen aggregates with `Object.entries(reduce)`
