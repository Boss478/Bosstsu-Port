# Task Report — Filter Active State Fix (2026-05-21)

## Completed
- Fixed "All" filter button not appearing active on first load for /games, /portfolio, /gallery
- Root cause: server returned `""` when no filter param, but client expected `"ทั้งหมด"`
- Changed server defaults from `""` to `"ทั้งหมด"` to match client button label
- No version bump (per user instruction)

## Files Changed
- `src/app/(website)/games/page.tsx` — `params.category || ""` → `params.category || "ทั้งหมด"`
- `src/app/(website)/portfolio/page.tsx` — `params.tag || ""` → `params.tag || "ทั้งหมด"`
- `src/app/(website)/gallery/page.tsx` — `params.tag || ""` → `params.tag || "ทั้งหมด"`

## Verification
- `npm run build` — passes

## Note
- Resources page already worked correctly (server returns `"All"`, client uses `"All"` button)
- All 4 pages now behave consistently: "All" button active by default on first load