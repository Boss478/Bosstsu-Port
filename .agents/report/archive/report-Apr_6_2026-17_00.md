# Report — Phase 1: MongoDB Indexes + Lazy Loading

**Version:** 1.5.1 → 1.5.2

## Changed Files

### `src/models/Gallery.ts`
* Added compound indexes: `{ published: 1, date: -1 }` (list queries) and `{ slug: 1, published: 1 }` (detail lookup)

### `src/models/Learning.ts`
* Added compound index: `{ published: 1, createdAt: -1 }` (list queries)
* Removed `index: true` from `published` field (redundant with compound index)

### `src/app/(website)/gallery/[id]/AlbumContent.tsx`
* Added `loading="lazy"` to all photo `<img>` elements — first few images load eagerly, below-fold images defer until scrolled into view

### `package.json` → version 1.5.2
### `changelog.md` → v1.5.2 entry added

## Notes
* `Portfolio.ts` already had all required indexes — no changes needed
* Pre-existing TS error in `admin/games/actions.ts` (unrelated to these changes)
