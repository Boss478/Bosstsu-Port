# Lane E — Task Report: Paper Surfaces (Wave 2) · 2026-08-04

Branch: `lane-e` (off `main`, Wave 1 merged). Plan: `krulaw-reading-redesign.md` §4.7/§4.8 — P8 + P6-parts (digest theme FAB + digest body-class) + §8.5 lane E.

## Git state (IMPORTANT for orchestrator)
- Lane-e commit: `340d40e` on branch `lane-e` — exactly 2 files (`KrulawListClient.tsx`, `DigestStudyClient.tsx`), lint/typecheck clean, prettier+eslint pre-commit hook applied.
- **Incident (resolved, no data loss)**: while I staged/committed, a concurrent lane had switched the shared working tree from `lane-e` to `lane-d`, so the commit briefly landed on `lane-d`. Fixed: `git branch -f lane-e 340d40e` (my commit now on lane-e) + `git reset --soft 6711701` + `git restore --staged .` on lane-d (pointer restored to Wave-1 main tip; ALL uncommitted lane-c/lane-d/release-prep working-tree changes preserved untouched, unstaged). Current checkout remains `lane-d` @ `6711701` (left as found — did not switch branches to avoid disrupting lane-d).
- Consequence: my two files also exist as unstaged working-tree mods on the current checkout, identical to commit `340d40e`. If another lane commits with `git add -A`, my files may ride along as identical content — harmless for merges (no conflicts on identical trees). No action needed; merge order per plan §8.5 (A+B → C+D+E).
- No `git push` performed (per task: push = ask).

## Files changed (exclusive ownership)
- `src/app/(website)/krulaw/KrulawListClient.tsx`
  - `krulaw-list-card` added to ALL 3 card variants: browse card (L285), metadata search-result card (L347), full-text search-result card (L369) — the `.read` paper hooks (globals.css, Lane A) now apply on list.
  - Contrast fix: search status line `text-slate-500 dark:text-slate-400` → `text-slate-600 dark:text-slate-400` (was 4.16–4.41:1 on paper; now 6.24–6.60:1). All other list muted text is `text-slate-600`-based and already ≥4.5:1 on paper (6.24–6.60 on `--read-bg`, or Lane A's `#6b5b3c` override inside cards at 5.75–6.11:1).
- `src/app/(website)/krulaw/digest/DigestStudyClient.tsx`
  - `krulaw-digest-card` on article cards (L160) — 76 cards on the live digest render paper via Lane A's `.read` override.
  - Digest theme FAB (new local `ThemeFab`): `krulaw-theme-fab` class (Lane D print-hide hook), `fixed right-6 md:right-10 bottom-32 z-50`, `h-11 w-11`, `aria-label` announces current→next in TH (`โหมดปัจจุบัน: {th} — สลับเป็น {next}`), icon per current theme (`fi-sr-sun`/`fi-sr-moon`/`fi-sr-book` — all in the P1 subset), cycles via `useTheme().setTheme` + `THEMES` const. Read mode is no longer a dead-end on digest.
  - `krulaw-immersive` body-class effect (post-hydration, cleaned on unmount) — the digest half of plan §4.7's client-owned body hook.
  - Contrast fixes (3 muted elements): strikethrough `<s>` `slate-400 dark:slate-500` → `slate-600 dark:slate-400`; "สารบัญมาตรา" label `slate-400` → `slate-600` (base); subtitle `slate-500` → `slate-600` (base). All now tone-safe: ≥5.42:1 on every paper tone AND improved in light mode (slate-400/500 on white was 2.56/4.36:1 — pre-existing AA fails), dark unchanged (slate-400 on slate-950 = 7.9:1).

## Acceptance results
| Criterion | Result |
|---|---|
| Paper tone on list + digest, all 3 tones | ✅ measured — card bg #faf3e3 / #f7efdc / #fdf6e3 per tone, border #e0d5b0, ink #3a2f1f; page bg #f5ecd9 / #f2e8d5 / #f6efd9 |
| Contrast ≥4.5:1 every muted element on `--read-card` | ✅ see ratio tables (min observed: list 4.85, digest 5.42) |
| Light/dark unchanged | ✅ light: white cards + blue-50 main; dark: slate-900 cards + slate-950 main (computed styles) |
| FAB cycles light→dark→read + persists | ✅ click sequence read→light→dark→read verified (html class + localStorage + aria-label + icon each step); reload keeps `read` |
| Back-link intact | ✅ present, `href="/krulaw"`, 5.62–6.33:1, navigation to list verified |
| `krulaw-immersive` set + cleaned | ✅ on digest = true; after navigating to list = false (unmount cleanup) |
| typecheck + lint | ✅ clean for lane-e files (pre-existing `SettingsMenu.tsx` errors are lane-c's in-progress file, untouched) |
| No console errors | ✅ zero React/hydration errors; one transient `/api/analytics` 500 during cold compile (site-wide endpoint, unrelated — 400 on empty POST, not a lane-e defect) |

### Measured contrast ratios (browser, canvas sRGB readback — WCAG 2.2)
List page (read mode): card ink **11.41–12.12** · card title **16.13** · card code line **5.75–6.11** · tag chip **6.02** · amber chip **4.85** · page count **6.24–6.60** · subject h3 **6.24–6.60** · part label **6.24–6.60** — across soft/classic/warm.

Digest (read mode): subtitle **5.42–5.73** · มาตรา label **5.42–5.73** · back link **5.62–5.94** · article label link **5.97–6.33** · jump chips **5.62–5.94** · article body **9.04–9.60** · strikethrough `<s>` **5.75–6.11** · h3 **15.51** · quote **6.35** — across soft/classic/warm.

Screenshots: `lane-e-list-read-{soft,classic,warm}.png`, `lane-e-digest-read-{soft,classic,warm}.png` (this dir).

## Deviations / notes
- **Environment**: the shared dev container had exited; restarted it via `docker compose --profile dev up -d app-dev` (per task). `/krulaw/digest` + `/krulaw/[slug]` 404'd due to a STALE `.next` named-volume (Turbopack route manifest predated the krulaw module) — fixed by clearing `/app/.next/*` in the container + restart. Also: container keeps exiting cleanly (restart policy `no` while compose says `unless-stopped`) — orchestrator should investigate; may recur for later lanes.
- Uncommitted working-tree changes from the release-prep effort (data compaction, `.prettierignore`, `package.json` dev script dropping `--webpack` → server now runs Turbopack) were present on `main` and carried into `lane-e`'s working tree UNTOUCHED — not committed by me.
- The shared Playwright browser is actively driven by other lanes (navigations/context resets mid-check); search-mode + console-error checks were re-run via a one-off headless Playwright script against the shared server (no new server started). Results in this report are from those runs.

## For Senior Review / merge handoffs
1. **Lane A (globals.css)** — genuine finding: the `.read [class*='krulaw-'] .text-slate-400` override color `#8a7a55` FAILS 4.5:1 on all three `--read-card` tones (measured 3.67–3.90:1). Recommend `#7a6845` (Lane A's own slate-500 override value — 4.71–5.00:1) or merging both selectors to one color. Lane-e files no longer carry slate-400 text in read mode, but OTHER lanes' krulaw surfaces (reader empty states, ReadingSettings, TocSidebar) still do and would get the failing color.
2. **Lane A** — out-of-scope: `SearchInput` placeholder (`text-zinc-400`) and `EmptyState` message (`text-zinc-500`, 4.22–4.48:1) fail on paper; not in my file ownership, not covered by the `.read [class*='krulaw-']` override (zinc, no krulaw-* ancestor). Suggest zinc-500/600 read overrides or file-level fixes.
3. **Lane D (print.css)** — `.krulaw-theme-fab` hide rule NOT yet present in print.css (checked 2026-08-04). The class hook is in place on the digest FAB; Lane D's P6 contract already includes the print rule — confirm it lands before merge or the FAB will print.
4. **Lane D** — reader body-class (`krulaw-immersive`) has no CSS consumers yet anywhere (grep: zero matches outside my effect). Harmless as a hook; confirm Lane D's print/offset rules key off it where intended.
5. Light-mode pre-existing AA fails fixed incidentally in my files (slate-400→600 on white: 2.56→6.6:1) — strict improvement, no design intent change (muted look preserved).
